# 看板更新的低开销流程

- 首先只检查四个来源的日期、重复键、关键汇总；不要把完整源表、整份 JSX、长 JSON 或浏览器树传入对话。
- 复用 scripts/read_distribution_snapshot.py 提取数据；只有异常指标需要进一步拆分。工具输出默认控制在 2000 token 内。
- 先完成所有数据、结论、日期和后续动作修改，再统一构建、一次发布；不要边检查边重复发布。
- 本周后续计划统一维护 src/distributionNextPlans.js，由相关页面引用，避免同一计划多处手改。
- 验收只检查最新日期、三类复盘、一个筛选交互及控制台；输出简短断言，不重复完整页面树。
- 不自动启用子代理，不修改模型或推理设置，不消耗额度重置。
- 超长任务历史会反复进入上下文。后续周期建议在同项目的新任务中读取本文件、最近提交及数据源，避免继续累积旧对话。创建新任务需用户明确要求。
- 用量调查仅输出 token 增量、缓存比例和主要大输出来源；日志 token 不是账户额度的直接计费换算。
